/**
 * Main application page
 * Renders the client component for the document assistant
 */

import DocumentAssistantClient from './client/page';

export default function HomePage(): JSX.Element {
  return <DocumentAssistantClient />;
}